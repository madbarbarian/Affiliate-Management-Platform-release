Here is what performed in the last {{lookbackHours}} hours on {{channelSummary}}.

## Posts collected ({{itemCount}} after filtering)

{{swipeItems}}

## Patterns already in the playbook

Do not re-propose these. If something you found is a variation of one of them,
set `mergesIntoExistingPattern` to that pattern's id and put the new evidence
against it instead.

{{existingPatterns}}

## What this account has actually published lately

Use this to judge fit. A pattern the writing team cannot execute in this voice
is not worth proposing.

{{recentOwnPosts}}

---

Extract the reproducible patterns. For each one, list the ids of the collected
posts that support it, so the claim can be checked later.
