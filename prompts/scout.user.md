Propose exactly {{count}} new accounts for this company.

## How this company thinks

{{principles}}

## What this company never does

{{boundaries}}

## The accounts we already run, and how they did

{{ventures}}

## Niches already taken - do not propose these or their synonyms

Running accounts and proposals still waiting for a decision alike.

{{existingNiches}}

## Categories you may name

`category` must be one of: {{categories}}. These are the company's offer
categories and the categories its markets restrict, plus "other". Pick the
honest one - a market's prohibited category is checked by this label.

## Markets we may publish in

{{markets}}

## Offers we have contracts for

Only these ids may appear in `offerIds`, and only for a market listed in their
`targetMarkets`.

{{offers}}

---

For each proposal give: `niche`, `audience`, `market` (an id from the list),
`language`, `category` (the main product category, one word), `ventureId` (a
short ASCII slug), three `nameCandidates`, a `voice` sketch, `offerIds`,
`hypothesis`, `evidence` (the numbers you leaned on, by account), `risk`,
three `firstHooks`, and `killSignal`.
