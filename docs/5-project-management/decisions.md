# 決めたことと、その理由

決定の記録。**何を決めたか、なぜか、どこに書いてあるか、何がまだ決まっていないか。**
新しい決定は下に足す。覆したときは消さずに「覆した」と書く — なぜ一度そう決めたかは、
次に同じ問いが来たときに効く。

設計そのものは各設計文書に、要件は
[要件定義](../1-requirements/requirements.md)にある。ここはその索引ではなく、
**判断の履歴**である。

---

## 2026-09 契約

### ライセンサーは個人。将来も個人事業のままでよい

法人格が要る場面は無い。屋号は契約の当事者にならないので署名欄は個人名と住所になる、
という実務上の違いだけ。法人化を考える引き金は4つ（無限責任・税・相手の購買規程・承継）で、
無償でテナントもいない現状はどれも当たらない。

**いま打った手は1つだけ。** 甲が自ら支配する法人へ契約上の地位を承継できる条項を、
契約A第7条と契約B第9条に入れた。これが無いと、法人化したときに
既存のライセンシー全員と結び直しになる。

→ [licensing.md 5.7](../1-requirements/licensing.md)、`contracts/`

### 準拠法は日本法・日本語。読者市場が海外であることとは別

読者が米国にいることは `markets:` の中身の問題で、契約の準拠法とは関係しない。
米国法人がライセンシーになる時点で初めて、英語版と米国側の法務が要る。
ひな型は日本語を先に確定させ、英語版は翻訳ではなく向こうで起草し直す。

### 当面は無償。対価はフィードバック

ライセンシーは自分の事業として運用し、その収益も、テナントに課金するならその料金も
ライセンシーのもの。ライセンサーは請求経路にも取り分にも入らない。
定額・成果連動の文案は契約B第4条の末尾に残してあり、切り替えるときは
消費税・インボイス・監査条項をそのとき整える。

**未決:** 無償契約での免責・責任上限の有効性は法務確認。ひな型は仮置き。

---

## 2026-09 どこで動かすか

### 既定は Cloudflare Workers。Docker の経路は残す

Vercel と比べた: 無料枠が**商用禁止**（アフィリエイトは確実に該当）、Hobby の Cron は
1日1回が上限で投稿の枠に届かない、Pro は $20/月。Cloudflare は無料枠に非商用条項が無く、
Cron は毎分、**課金が CPU 時間なので待ち時間が数えられない** — 1サイクルの大半は
モデルの応答待ちなので、いちばん高くつくはずの部分がタダになる。$5/月。

無料プランは 10ms CPU で足りない（設定を読んでスコアを計算するだけで超える）ので、
現実解は Workers Paid。

**Worker は入口を1つ増やすだけで、置き換えではない。** `daemon` はそのまま動く。
`workerd` は Node ではないので CLI は手元に残る必要があり、実行環境は2つになる。

→ [hosting.md](../4-operations/hosting.md)、[cloudflare-design.md](../3-development/cloudflare-design.md)

### 保存先は SQL。3テーブル、1行1 JSON

型ごとの列を作った瞬間に、型を変えるたび移行が要る（未決事項 14）。
1行1 JSON なら、いまのファイル版と同じく型の変更に移行が要らない。
`find(predicate)` は JavaScript の述語なので SQL に翻訳する道が無く、
ファイル版と同じく全件読んでメモリで絞る — **挙動が変わらないことが唯一の約束**。

`seq` は並び順のためだけにある。upsert は書き換えないので、差し替えても末尾に動かない。

**判明したこと:** `node:sqlite` が Node に内蔵されている。D1 は SQLite なので、
**本番で D1 が実行するのと同じ文をテストで本物の SQLite に対して走らせられる。**
依存は増えない。設計時に想定していた「フェイクドライバ」は不要になった。

### 停止と非アクティブはポートの向こうへ。安全性の判断はポートの外

`StateStore` の `read` は**同期のまま**。tick は公開する前にこれを見るので、
`await` を挟むと停止を見落とす窓ができる。DB 版は `refresh()` が読んだ
スナップショットから答え、**最初の成功した refresh までは全部 `unreadable`** —
つまり停止扱い。refresh を忘れた入口は止まる側に倒れる。

「読めないときどうするか」は保存先の都合ではなく安全性の本体なので、
`pause.ts`（fail-closed）と `venture-state.ts`（fail-open）に残した。

### `--dry-run` は「何が動いているか」の答えを持たない

一度これを間違えた。`--dry-run` に空の状態ストアを渡したせいで、実機が停止中でも
`doctor --dry-run` は「動いている」と答えた。**確認したいから dry run を打つのに、
実機と違うことを言うのは無意味以下。** 保存先・モデル・アダプタは差し替える。
停止と非アクティブは差し替えない。

→ 実バイナリを起動するスモークテストで固定（`test/cli.smoke.test.ts`）

**未決:** `cycleStartsAt` の「分」が Workers で無視されること（重いサイクルを
1時間 Cron に置いて CPU 15分枠を取るため）を受け入れるか。

---

## 2026-09 ライセンシーの最初の1時間

### 受け取るのはリンク1つ。clone も YAML も出てこない

Deploy to Cloudflare ボタンが、リポジトリの複製・D1 と Durable Object の作成・
秘密の入力・ビルド・配置・CI/CD をやる。こちらが用意するのは
`wrangler.jsonc`、`.env.example`、`package.json` の説明文、README の1行。

### ドメインは要らない

`workers.dev` のアドレスが無料で公開されるので、`/go/` が初日から動き、
**計測経路が初日に閉じる**。旧ステップ9（ドメインの用意、30分〜、脱落率が最も高い）が
丸ごと消えた。Cloudflare 自身は workers.dev を事業の中核には勧めていないので、
案内は「始めるには十分、育ったら独自ドメインへ」。

### デプロイ画面でモデルの鍵を必須にしない

必須にすると「**鍵を1つも持たずに製品が動くところまで行ける**」という設計の核が、
いちばん最初の画面で壊れる。**合言葉は必須** — 同じアドレスで承認画面が開くため、
そこは後回しにできない。

**2026-09-08 に本当になった。** それまでは半分嘘だった — 出荷している例の設定が
`llm.provider: anthropic` だったので、鍵を入れるまで承認画面が開かなかった。
実デプロイの1画面目で発覚。例を `mock` に変え、鍵は手順4で入れるものにした。

見つからなかった理由が本題で、**スモークテストが例の設定を `mock` に書き換えてから
試していた**。「壊れた例の設定は誰かの初日ではなくここで落ちる」ためのテストが、
落ちようのない形に直してから走っていた。書き換えを外すと12件中10件が赤になる。
README はこの問題を知っていて、直さずに回避策（`--dry-run` を足せ）を書いていた —
端末があれば回避できるが、Cloudflare には端末が無い。

### 設定は承認画面が保存しない。ライセンシーが貼る

Workers のファイルシステムが読み取り専用だから、ではない。**設定はライセンシーのもので、
変更の履歴は本人の控えに残るべき**だから。探索担当の提案を採用したときも同じ経路を通る。

→ [onboarding.md](../2-setup/onboarding.md)、`docs/_proposed/design/onboarding/`

### 壊れていても、リンクと死活だけは答える

`/go/<code>` は、設定が検証に落ちても、合言葉が無くても答える。公開済みの投稿に
入っているリンクは取り消せないし、**記録されなかったクリックは後から数えられない**。
そのためにリダイレクトは `Store` と id と時計しか要求しない（`RedirectDeps`）。
`/healthz` はデータベースより手前。

### Cron の重複はロックで止める。実装は Durable Object

Cron は前回の実行中に発火しうるし、再試行もされる。ロックが無ければ同じ投稿が
2回出る。Durable Object は名前ごとに1つしか存在せず、1度に1リクエストしか扱わない
ので、**それ自体がロック**である。ロックは保持者を記録する — TTL を超えた保持者が
遅れて release したときに、その後に取った人のロックを外さないため。

### ライセンシーのフォークでは、設定を commit する

こちらのリポジトリでは `platform.config.yaml` は git-ignore のまま（実アカウントが
入るため）。**ライセンシーのフォークでは ignore しない。** Cloudflare ではビルドが
設定を Worker に埋め込むので、commit されていない設定は**デプロイが一度も見ない設定**で、
例のまま立ち上がる。リリーススクリプトが `.gitignore` を書き換え、理由もそこに書く。

設定に秘密は入らない（環境変数の名前だけ）という規則があるので、commit しても安全。
これが成り立たなくなったら、この判断も崩れる。

### 検証はアカウント無しでほぼ済んだ（2026-09-04）

`wrangler dev --local` が本物の `workerd` とローカル D1 を立てるので、
「デプロイしないと分からない」と書いた2つはアカウント無しで潰れた。
バンドルは通り（790 KiB）、SDK を含むモジュールグラフは起動し、**1日ぶんの流れが
丸ごと `workerd` の上を通った**（企画10案 → 承認 → 執筆 → 検品 → 枠決め）。

残るのは、本番の鍵が要る Anthropic への実呼び出しと、
ホストされた側（`workers.dev` のアドレス、Cron の実発火、Deploy ボタン）。

### 実デプロイで残り3つのうち2つが片付いた（2026-09-08）

`amp.madbarbarian.workers.dev`。**両方の Cron が自力で発火し**、1日ぶんが
ホストされた側を通った — 毎時Cronが企画からゲートまで、承認のあと執筆・検品・枠決め、
毎分Cronが枠の来た1件だけを公開。ティックが何度走っても公開イベントは1件のまま。

**`/go/` のクリックが1件記録された。** 「ドメインは要らない」は、これで実機の裏付けが
ある。旧ステップ9を消した根拠がここで確定した。

**3番目の画面でいきなり止まった。** 例の設定が `llm.provider: anthropic` で、
鍵が無いと承認画面が開かなかった。スモークテストが例を書き換えてから試していたので
誰も気づいていなかった。→ 別項

残るのは実ストリーミング呼び出しと Deploy ボタンの経路。

---

## 2026-09 検証を誰がやるか

### 参照環境は自分で持つ。$5 は検証費用ではなく設備

ライセンサーは運用者ではないので、「先行利用者（知人）に動かしてもらいながら
直せばいい」という選択肢があった。**採らない。** 順番を決めたのは費用ではなく、
2つの検証が別のものを試しているという点。

**インフラの不具合はフィードバックではない。** 残っている workers.dev の
アドレス・Cron の実発火・実ストリーミング呼び出しは、動くか動かないかの話で、
相手に見つけさせる種類のものではない。一方**オンボーディングが分かるかどうかは、
書いた本人には試せない** — 初見の目は一度しか使えず、それをバグの発見に
使うのは高すぎる。

**相手の側では目が見えない。** Cloudflare には端末が無いので `doctor` が無く、
相手のアカウントの `wrangler tail` も見られない。動いているものを一度も
見ないまま、動かないものを直すことになる。

順番はどのみち強制されてもいる。ボタンには公開リリースリポジトリとリリース発行が
要り、まだどちらも無い。自分のデプロイはいますぐできる。

無料プランで代用しない。CPU 10ms で落ちたときそれがコードのバグに見えるので、
**曖昧な結果は無い結果より悪い。**

$5/月は1回の検証費ではなく、ライセンサーである限り要る**参照環境**の維持費と
考える。「壊れた」と言われたときに突き合わせる先が無い状態には戻らない。

→ [deploy-checklist.md](../4-operations/deploy-checklist.md)

### 自分の D1 の id は、リリースに乗るファイルに書かない

`wrangler.jsonc` はライセンシーにそのまま届く。id をそこに書くと**全員が
こちらのデータベースを指す設定を受け取る**（開けはしないが、設定としては誤り）。

最初の手順書は「placeholder に戻してから release を切る」と書いていた。
**戻し忘れる手順は、いつか忘れる。** `npm run deploy` が `.env.local` の
`AMP_D1_DATABASE_ID` を見て一時ファイルを作るようにして、戻す手順自体を消した。
変数が無い経路 — つまりライセンシー — は以前と1文字も変わらない。

id は秘密ではないが `.env` ではなく `.env.local` に置く。`.env` は commit される。

---

## 2026-09 オンボード

### 初期設定は生成器にする。設定ファイルを画面が持ち続けることはしない

ライセンシーの初日は428行の YAML を開くところから始まっていた。
[オンボード設計](../2-setup/onboarding.md)がそこを最大の脱落点と名指ししている。
6問に答えると設定ファイルができあがる形にした（v0.3.0 以降）。

**テキストを埋める。オブジェクトを書き戻さない。** 428行の大半はコメントで、
なぜその値なのかを説明している。パースして書き戻すとそれが消える。
これは承認画面を設定の編集器にしない理由と同じもので、要件3.1に決定として記録した。

### ライセンシーのリポジトリへの書き込みは、いまも「しない」

**貼り付け1回は残す。** 2026-09-12 判断。

中身を URL に載せる案は実測で否定した。GitHub の新規ファイル画面はクエリで本文も
受け取れるが、GET の上限が約2KB、この設定ファイルは URL エンコードで41KB
（日本語がパーセントエンコードで1文字9文字になる）。**ファイル名だけは載る**ので、
「Add file → Create new file → ファイル名を打つ」の3手順は消した。

自動でコミットするには資格情報が要り、形は2つしかない。

| 案 | 状態 |
|---|---|
| 私たちがホストする GitHub App | **却下**（[更新を受け取る](../3-development/taking-updates.md) 4節）。理由はいまも有効 |
| ライセンシーが発行したトークンを自分の Cloudflare に置く | **見送り。** 2つのダッシュボードにまたがる約15手順で、途中で権限スコープを選ぶ必要がある。トークンを知らない人には貼り付け1回より難しい |
| **GitHub Device Flow** | **将来の選択肢。** 下記 |

### 「読むのは画面、変えるのはファイル」は、いまのところ妥協である

**2026-09-12、オーナーからのフィードバック:**「結局、編集はファイルで、になってるんだね。いまいちだよね。」

**そのとおりである。** 上の2つの理由（コメントが消える／真実が2箇所になる）は本物だが、
**理由があることと、使いやすいことは別**である。ライセンシーから見れば
「設定画面があるのに、設定できない」で、それは変な製品である。ここを
「設計上正しいので問題なし」と記録すると、次に読む人が問題を問題として見なくなる。

いまの形は「読む」と「変える」を別の場所に置いた結果で、一貫はしているが親切ではない。
**今回はこれで試すと決めた**（オーナー判断、2026-09-12）。試した結果が、次の判断の材料になる。

**本当に解くなら、画面から変えて、その変更が git のコミットになる形である。**
真実は1箇所のまま、履歴も残る。ただしそれには「ライセンシーのリポジトリに書ける権限」が要るので、
**この課題と下の Device Flow は、実は同じ1つの課題である。** 別々に解こうとしないこと。

### 将来: GitHub Device Flow（未着手）

`github.com/login/device` にコードを入れて承認するだけで、ワーカーがライセンシーの
リポジトリに設定ファイルをコミットできるようになる。貼り付けが消える。

**却下した App 案とは別物である。** 発行されるトークンはライセンシー本人のもので、
1人分だけ、本人がいつでも剥がせる。私たちは何も保持しない
（Device Flow はクライアントシークレットを使わない）。
要件6節「ライセンシーが自分の手元にあるものを監査する」は保たれる。

**いま作らない理由:** ライセンシーがまだ1人もいない。貼り付け1回が本当に脱落点なのかは、
誰かが実際につまずいて初めて分かる。確かめていない問題に工数を使わない。

**着手する条件:** (1) ライセンシーが実際にここで詰まった、または
(2) 最初の1人に見せる体験として、貼り付けを残したくないという営業判断。

---

## 2026-09 最初のライセンシーに渡す前に

### 鍵ゼロで試せる範囲を、先に人に試してもらう

> **2026-09-16 改めた。** 模擬モードでは「出てくる案が読むに値するか」に答えが出ないと分かり、
> 最初のテストは**本物のモデル**で行うことにした。→ [最初のテストで確かめること](#最初のテストは本物のモデルで投稿はしない)。
> 下の「本物のSNSはその結果を見てから」は変わらない。

**2026-09-12 決定。** 最初のテスト利用者（オーナーの配偶者）には、
**模擬モードで最初の1時間と最初の3日**を試してもらう。本物のSNSとASPは、その結果を見てから。

理由は2つ。

- **`threads.ts` は一度も動いたことがない。** 実装はあるが、HTTPの経路をテストが
  一度も通っていない（[外とつながる部分](../3-development/integrations.md)）。
  動いたことのないコードで実アカウントに投稿すると、失敗したときに
  「製品が悪いのか設定が悪いのか」が切り分けられない。**最初の1人の信頼は一度しか無い**
- **ASPの審査に数日かかる。** どのみち今日は始められない

**そして、鍵ゼロで確かめられることが、まだ一度も人に試されていない。**
貼り付けが本当に脱落点なのか、出てくる案が読むに値するのか、毎日の承認が30秒で
終わるのか。[オンボード設計](../2-setup/onboarding.md)が「鍵を1つも持たずに
製品が動くところまで行けること」を第一原則に置いたのは、まさにこれを確かめるためである。

### Cloudflare では国内ASPの成果を取り込めない（未解決）

`csv` アダプタは `node:fs` を読む。**Workers にファイルシステムは無い。**
要件3.1は「ライセンシーは端末を持たない」と決めている。この2つが重なると、
**Cloudflare のライセンシーは A8.net の成果を1件も取り込めない。**

既定の置き場所を Cloudflare に寄せる判断（未決事項15）をしたとき、
**この帰結が検討されていなかった。** 要件3.1の規則
「配布方法・実行環境を変える判断は、必ずこの表に戻る」が、また同じ形で効いている。

案は3つ（画面からCSVをアップロード／ASPのAPI／メール転送）。
比較は[外とつながる部分](../3-development/integrations.md) 4節。**まだ決めていない。**

### Amazon アソシエイトは、構造ではなく規約の問題

ポートは Amazon を受け入れられる（`buildTrackedUrl` がアソシエイトタグの穴）。
**問題は運営規約がリダイレクトによる隠蔽を禁じていること**で、このプラットフォームは
全てのリンクを `/go/<code>` に通す設計である。

「Amazon だけ直リンクにする」は技術的には簡単だが、**「計測はこちらが持つ」という
設計の一部を Amazon に対してだけ降ろす**ことを意味する。**法務判断であり、
勝手には決めない。** 未決事項として扱う。

> 🔴 **訂正（2026-09-22）— 「しうる」ではなく、確定した。**
>
> 上の記述と、下の表の「Amazon はリダイレクタを問題に**しうる**」は、二次情報に基づく
> 推測だった（当時は公式ページがネットワーク制限で読めなかった）。**今回は一次情報を
> 読めた** — `affiliate-program.amazon.com/help/operating/policies` と
> `affiliate.amazon.co.jp/help/operating/policies` の両方。
>
> - 第6条(v)：**リダイレクトページの利用を含め**、参照元URLを不明瞭にしてはならない
> - 第6条(w)：Amazon 行きと分からない短縮リンクは不可
> - 料率表 2(e)：**能動的行為を伴わず仲介サイト経由で送るリンク経由の購入は、適格販売から除外**
> - 和文：「全ての特別リンクは、乙のサイトから直接アクセスしなければなりません」
>
> **「問題にしうる」ではなく「規約違反であり、かつ報酬が出ない」。**料率表の除外は、
> 計測できないという話ではない。**1円も入らない。**これは法務判断を待つ性質の
> 不確かさではなくなった。残る判断は「Amazon を扱うかどうか」であって、
> 「リダイレクタが許されるかどうか」ではない。
>
> **A8.net は未確認。**メディア会員規約 8.1.12 と禁止事項を一次で読んだが、
> 「リダイレクト」「転送」「短縮」の語が**0件**だった。書いていない＝許されている、
> とは読めない。**A8 に直接訊く必要がある。**これは小さな確認ではない —
> **A8 も直リンクを要求するなら、`direct` は例外ではなく主流になり、
> 「クリックはこちらで数える」という設計の前提そのものが変わる。**

### この規約が、実際に何を壊していたか（2026-09-22）

本文のリンクが初版から直リンクだったのを直した日（PR #70）に、逆向きの問題として現れた。
**Amazon にとっては、直リンクこそが正しい。**いまの `main` は「全案件が `/go/` 経由」で
筋が通っているが、**Amazon の案件を1つ登録した日に、静かに壊れる** —
設定は通り、リンクは発行され、投稿は出て、**報酬だけが入らない。**

判断を2つに割った。

| | 中身 | 状態 |
|---|---|---|
| **B2a** | Amazon と判定できる案件を、**設定の検証で拒否する**。`direct` を作る前の fail-closed な足止め | **着手（2026-09-22 オーナー了承）** |
| **B2b** | 案件・ネットワークごとの `linkMode: redirect \| direct`。既定は `redirect`。直リンク案件は計測欄で**緑でも赤でもない第3の状態**（「クリックはこちらでは数えません」） | **保留。**Amazon を試す日、または A8 の回答が来た日 |

**自動判定について。**`offer.landingUrl` はネットワークが包む前の商品URLなので、
ホスト名で判定できる。ただし**「設定に `redirect` と書いてあるのにコードが黙って
`direct` にする」形は採らない** — それは今日この repo で潰したばかりの
「画面（設定）とコードが違うことを言う」欠陥そのものであり、検知が外れたときに
外れたことが誰にも分からない。**危ない組み合わせを拒否する**形にする。
そして**ホストの表は表に在るものしか捕まえない**ことを、コードに明記する。

---

## 2026-09 ライセンシーが払うもの

### Workers Paid $5/月は前提条件。「無料」は誤りだった

**Workers Free の Cron は CPU 10ms。** [どこで動かすか](../4-operations/hosting.md) 2.2 が
「設定の YAML を読んでスコアを計算するだけで超える。リダイレクトだけなら無料で足りるが、
サイクルは無理」と、**正しく算定していた**。

にもかかわらず[オンボード設計](../2-setup/onboarding.md)は「Cloudflare のサインアップ（無料。カード不要）」
と書いていた。**ライセンシーに最初に伝わる条件が、そこだけ間違っていた。**

訂正済み。要件3.1の能力表に「月額を払う」を1行足し、失敗の5件目として記録した。

**リンクのリダイレクトとクリック記録は無料でも動く。** 動かないのは1日のサイクルである。
この区別は、無料で始めたライセンシーが「何かは動いているのに提案が出ない」状態になったとき効く。

### 今日3度、答えが既にリポジトリの中にあった

2026-09-12〜14 に「発見」した3件は、どれも記録済みだった。

| 「発見」したこと | 実際はどこに | いつから |
|---|---|---|
| `threads.ts` は本物を相手にしたことがない | [モックが見ていない帯域](../6-testing/bands.md) | 2026-09-10 |
| Amazon はリダイレクタを問題にしうる（🔴 2026-09-22 訂正: 「しうる」ではなく確定。上の節を見よ） | 要件 未決事項3 | それ以前 |
| 無料プランではサイクルが回らない | [どこで動かすか](../4-operations/hosting.md) 2.2 | それ以前 |

**記録は足りていた。読まれていなかった。** CLAUDE.md は「先に読み、ゼロから考え直すのではなく、
既に決まっていることを述べよ」と指示しており、守られなかったのはその手順である。

**ここから引く教訓は「もっと記録する」ではない。** 記録は既に十分だった。
必要なのは、調べ始める前に `docs/` を引くことと、**同じ問いに2つの文書が答えているとき、
ライセンシーが読むほうを正とすること**である。今回ずれていたのは、まさにそちらだった。

---

## 2026-09 Amazon アソシエイトを調べた

### Amazon は初日に使えない。「読者がいる人」向けの制度である

オーナーが「Amazonアフィリエイトってどうやるの、やったことがない」と訊いたので調べた。
**A8.net と同じ順番で扱えないことが分かった。**

登録しただけでは審査が始まらない。**180日以内に、自分のリンク経由で他人が3件買う**ことが
審査の受験資格になる。自己購入は数えず、家族・友人に頼むのは規約違反である。

**これは製品の順番の話になる。** 最初のライセンシーに「アフィリエイトを登録してください」
と言うとき、A8.net（審査に通れば読者0人でも案件を持てる）と Amazon（読者がいないと
180日で失効する）は**同じ日にできる作業ではない**。オンボードで並べて見せると、
Amazon 側が必ず失効する。

順番は **A8.net で案件を持つ → 読者ができる → Amazon を足す**。

### 日米どちらもやるなら、Amazon 側の条件は2倍になる

ライセンシーが日本と米国の両方を視野に入れていると分かった。

**プラットフォーム側は既に答えが出ている。** [越境の設計](../7-references/cross-border.md)が
「市場が2つならアカウントを2つ」「コンプライアンスは読者に従う」「`targetMarkets` の外は
設定時エラー」「通貨は足さない」を決めており、設定ファイルにも `us_main` の雛型が
コメントで入っている。**ここは追記であって、コード変更ではない。**

増えるのは Amazon 側である。Amazon.co.jp と Amazon.com は別プログラムで、登録も審査も
タグも支払いも別。**3件の壁が2回ある。** 読者が日本に偏っていれば米国側は埋まらない。

OneLink（各国のアカウントを紐づけて、読者を自国のストアへ送る仕組み）はあるが、
**紐づける先には先に通っている必要がある** — 審査を省く道具ではない。

**推奨は「最初のテストは日本1市場から」。** 3件の壁を2回同時に越えるのは、
いちばん難しいところを倍にして注意を半分にする動きである。米国側は後から
`ventures:` への追記で足せる。

### 確かめていないことを、確かめたと書かない

Amazon 公式の `affiliate.amazon.co.jp` は、開発環境のネットワーク制限で直接読めなかった。
上記のうち**紹介料率の細目・OneLink の挙動・対象SNSの一覧は二次情報**である。

とくに**対象SNSに Threads が見当たらない**件（未決事項19）は、事実なら既定のチャネルと
Amazon が両立しないという重い話になる。それを二次情報のまま設計に反映するのは、
この3日間に3度やった失敗（既にある記録を読まずに動く）の裏返しの失敗になる。

**登録画面を5分開けば確定する。** 確定するまで設計は変えない。

---

## 2026-09 最初のテストで確かめること

### 最初のテストは本物のモデルで、投稿はしない

**2026-09-16 オーナー決定。** 最初のテスト利用者に確かめてもらうのは4つ。

1. **使うと決めた人が、デプロイボタンから自分で立ち上げられるか**
2. **ASPのアカウントを作り、SNSのアカウントを用意できるか**
3. **自分の案件に沿った投稿案が出てくるか**
4. **その投稿案が、面白いか・役に立つか**

**最初は投稿しない。** 出てきた投稿案を、利用者に読んで評価してもらう。
**Cloudflare と LLM の契約は、最初のテストではオーナーのものを使ってよい** — 動くかを見るのが先で、
利用者に費用やカード登録を求めるのはその後でよい。

**9/12 の決定（模擬モードで最初の3日）を改めた理由。** 調べると、模擬モードの提案は設定と無関係な
固定文だった（`src/llm/demo.ts`）。ニッチも文体も案件も反映されないので、4 には答えが出ない。
**確かめたいことのうち、いちばん製品の価値に近いものが、模擬モードでは測れなかった。**

**変わらないこと。** 本物のSNSへの投稿は、この結果を見てから。`threads.ts` の HTTP 経路は
一度も動いていないので、「投稿しない」は目的であると同時に安全策でもある。

**この決定から出てくる前提（まだ確かめていない）:**

- **1 と「オーナーの Cloudflare を使う」は、そのままだと両立しない。** ボタンは、押した人が
  ログインしている Cloudflare アカウントに立ち上げる。利用者をオーナーのアカウントの
  メンバーとして招待すれば両立するはずだが、**メンバーの権限でボタンが最後まで通るかは未確認**
- **3 には本物の案件が要る。** セットアップ画面が作る設定には、`example.com` を指す
  サンプル案件が1つ入っているだけ。案件は設定ファイルに書く（`offers:`）ので、
  利用者は **GitHub 上でファイルを編集する**ことになる。ここ自体がオンボードの確認対象になる
- **4 の感想を残す場所が画面に無い。** 承認の記録には `note` が型としてあるが、
  承認画面から書く入口は見当たらない

---

## 2026-09-19 渡す前の修正をどこまでやるか

v0.7.0 を出した翌日の調査で、4件の不具合と2件の環境の問題が確定した。
**どれをどう直すかをオーナーが決めた**（2026-09-19）。

### 設計キャンバスの公開版は、同じURLでなくてよい

再シードに使っていた `seed-canvas.mjs` と `payload.template.html` が Claude の
design skill から消えた。**正本（`.dc.html` と `canvas.json`）はこのリポジトリにあるので
失われていない**が、公開版を作り直す手段が無い。

**URLの同一性は要件ではない**とオーナーが決めた。よって選択肢は「道具が戻るのを待つ」
ではなく「配り方を選び直す」になる。

**続けて決めた（同日）: 自前で描き出す仕組みを持つ。**

板は24枚中22枚が静止で、`<x-dc>` の動く仕掛けを使っているのは `Main` と `Publish` の
2枚だけ — **自前で持てる範囲のほうが広い。**`scripts/build-canvas.ts` が `canvas.json` と
`.dc.html` から静的HTML1枚を生成し、**出力をリポジトリに入れる**。依存は足さない。

**失うもの:** `Main` と `Publish` の対話（チェックを入れる・並べ替える）。
**得るもの:** 消えない。差分が読める。GitHub で開ける。
「キャンバスはコードと一緒に動く」が、比喩ではなく文字どおりになる。

**外部の道具に依存し続けないと決めた理由は、URLではなく寿命。** 同じURLを保つことは
要件ではないと確かめた上で、それでも作り直しを選ばなかったのは、**同じ理由でまた消えるから**。

### 停止中のアカウントは「動かさない」を1か所で決める

個別ページに判断待ちが出ない件（画面側の欠陥）を直す過程で、2つ目が出た。
**`buildState` は停止中アカウントの判断を除外するのに、バッジは除外しない** —
実測で「バッジ1・中身ゼロ」。

**別々に直さない**とオーナーが決めた。稼働しているかどうかの判断を**1か所で答えさせ**、
バッジも一覧も個別ページも、そこに聞く。`market.ts` が越境の答えを1か所で出すのと同じ形。
**同じ問いに2つの答えを持たせない**、というこのリポジトリが3度繰り返した失敗の予防でもある。

これは STATUS.md の「停止したアカウントが、まだ判断を求めてくる」（開いているゲートが
閉じない）と同じ根なので、一緒に直す。手動投稿が v0.7.0 で入ったので、待つ理由は消えた。

### 1日の試行上限は config に置く。ただし費用への効き方を書く

毎時やり直しの修正で、試行上限を定数にするか設定値にするかが残っていた。
**config にする**とオーナーが決めた。「ライセンシーが変えたいものは config か `prompts/`」
という既存の決まりに素直に従う。

**条件が1つ付いた:** この値は**ライセンシーの請求額に直接効く**ので、
上げ下げが費用にどう響くかを、設定の説明と画面の両方で言えるようにすること。
黙って上げられる数字にしない。

### 文言の修正は、件数ではなく「嘘かどうか」で優先する

承認画面の端末コマンドを洗い出したところ、`amp scout` 2件より広かった。
だが**いちばん重いのは件数ではなかった** — `scout.empty` の
「`amp scout` で探索担当に出させられます」は、**画面からは不可能**である。
探索担当の起動経路は `tick.ts` の `exploreIfDue` 1つだけで、
既定の `exploration.enabled: false` では一度も動かない。

**要件3.1（端末を持たない）との衝突より、嘘であることのほうが重い**という順序で直す。
端末に触れない文言に書き換えるだけでは足りず、**画面からできないことを「できます」と
書かない**ところまでを直しとする。

### 道具が壊れているなら、道具を直す

2件とも「回避する」ではなく「直す」と決めた。

- **`src/kernel/exploration.ts` を `grep` が黙って飛ばす。** 文字クラスに生の NUL が
  埋まっていて `file` が `data` と判定する。**コード検索の恒久的な死角**で、実際に
  この日の調査で踏んだ。`\u0000-\u001f` に書き換える
- **`npm run check` がオーナーの機械で落ちる。** **原因は「パッケージが足りない」では
  なかった** — `sysctl.proc_translated` が 1、つまり**シェルが Rosetta で起動していて**、
  universal binary の node が x86_64 側で動き、tsc が x64 版を探していた。ハードは M4。
  **x64 版の tsc を入れる（＝足りないものを入れる）のは採らない。**入れれば通るが、
  M4 を丸ごとエミュレーションで動かし続けることになる。実測で `arch -arm64` は
  9.5秒、Rosetta 経由は 37秒 — **約4倍**。直すのはターミナル側の
  「Rosetta を使用して開く」であって、リポジトリ側に回避策は入れない。
  **入れると、エミュレーションで動いていることが見えなくなる**

---

## 2026-09-20 `_drafts` という名前が、品質の基準を免れさせていた

> **改名は実施済み（2026-09-20）。** このリポジトリの `docs/_drafts/` は
> `docs/_proposed/` になった。以下に出てくる `_drafts` は、**改名前の状態**と、
> 提案の宛先である `dev_base` 側の名前を指す。現在のパスではない。

設計キャンバスをライセンシーに配るかを検討していて、**配る配らない以前に名前が
間違っている**と分かった。

**下書きは「まだ見せられないもの」、提案は「見せるために作ったもの」。**
`docs/_drafts/` に置いているのは後者で、しかも**実際に配布されている**
（`make-release.ts` の `INCLUDE` に `docs` があり、`test/release.test.ts` が
「`_drafts/design/` は配る／未コミットの走り書きは配らない」を固定している）。
**振る舞いはテストで守られていて、名前だけが取り残されていた。**

**害は名前の気持ち悪さではない。基準が適用されないことだった。**
承認画面から端末コマンドを取り除いた同じ日、`_drafts/design/onboarding/Connect.dc.html` の
**画面コピー**が `amp setup --resume` を案内していた。要件3.1違反が、
「下書きだから」という理由で誰の検査対象にも入らないまま配布されていた。
実装済みと未実装が無印で同じ棚に並んでいたのも、同じ根である。

### 逸脱として記録せず、上流（`dev_base`）を直す（2026-09-20・オーナー）

**`CLAUDE.md` の「逸脱とその理由」に書き足す形は採らない。**
逸脱として各リポジトリに書き残すと、**同じ問題を次のリポジトリがまた踏む。**
7分類レイアウトを定義している `dev_base` 側の定義そのものを直しにいく。

提案書は [`docs/7-references/dev_base-proposal-drafts-vs-proposed.md`](../7-references/dev_base-proposal-drafts-vs-proposed.md)。
**まだ渡していない。**

### 改名の前に確かめること → 確かめた。安全だった（2026-09-20）

改名は下流では**「旧ディレクトリの削除 ＋ 新ディレクトリの追加」**として届く。
更新の仕組みが削除を伝えない実装なら、ライセンシーの手元に旧 `_drafts/` が残り、
新旧2つの同じキャンバスが並ぶ、と**懸念していた。実測で覆った。**

2つのホップが**両方とも `rsync --delete`** で、`sync-to-release.yml` のコメントは
理由まで書いていた —「上流で消えたファイルは下流でも消す。ライセンシーのフォークに
取り残された孤児モジュールは、欠落より悪い」。改名した状態で1回同期すると、
削除と追加が同じコミットに乗り、旧ディレクトリは消えた。

**訂正として残す。** 確かめずに「危険かもしれない」と書くのは、
確かめずに「安全だ」と書くのと同じ質の誤りである。

### ⚠️ 代わりに、別の穴が見つかった

同じ実測で、**ライセンシーが自分で足したファイルが更新で消えた**（`MY-NOTES.md`）。
`--exclude` は4つだけで、それ以外は `--delete` の対象になる。
文書はその4つを「意図的に触らない」と挙げるだけで、**それ以外が消えるとは書いていない。**
**この振る舞いを固定したテストも無い。**

改名とは別件。渡す前の妨げにはならないが、運用に入ったライセンシーには効く。

---

## 2026-09-20 サブスクの契約で生成させられないか（見送り。理由は記録する）

**オーナーの問い:**「テスターも自分も Claude Max や Gemini Pro を契約している。
API の従量課金ではなく、そちらに生成させられないか。」

**製品としては採らない。**ただし理由は「難しいから」ではないので、書き残す。

### 採らない理由は2つ。どちらも技術ではない

1. **契約の形が違う。** Max / Pro は**契約者本人が、提供元の画面を通して対話的に使う**
   ための契約で、第三者の自動サービスの裏側として動かすのは利用条件の外である。
   そしてこれは**ライセンス製品**なので、土台が利用条件違反だと、
   **その違反ごとライセンシーに相続させる**ことになる。

2. 🔴 **動く場所が合わない。これが決定的。**
   この製品の核は**誰もいない時間に Cron が起きてサイクルを回す**ことで、
   一方サブスクは**ログインした人間のセッション**に結びついている。
   Worker が持てる「Max の資格情報」は存在しない。
   **「人がいなくても動く」という製品の定義そのものと噛み合わない。**

### ただし、問いの中に本物の変種がある

**人がいる前提なら成立する。** 運用者はどのみち1日1回、承認画面を開く。
`src/llm/` は差し替え口になっていて（いま `anthropic` と `mock`）、3つ目を足せる。

**失うもの:** 自律性（「寝ている間に会社が動く」が消える）と、
**要件3.1**（ライセンシーは端末を持たない）。Claude Code を動かす機械と契約が要る。

**つまり別の製品である。**悪い案という意味ではなく、**売っているものが変わる。**

### economics は、思ったより小さい

| | |
|---|---|
| Cloudflare | **$5/月**（必須。サブスク経路にしても消えない） |
| AIモデル | 実測で**毎日 $0.3〜0.6**（月 $9〜18） |

9/16 の $4.2 の跳ねは、**2026-09-20 に直した毎時やり直しのバグ**が原因だった。
上限（`company.retry.maxCycleAttempts`）を入れたので、同じ跳ね方はしない。

**削れるのは月 $10 前後**で、そのために自律性と要件3.1を捨てる取引になる。

### 開発者側では試す（2026-09-20 オーナー）

**ライセンシー向けには採らないが、開発者はどのみち画面の前にいる**ので、
切り替えを試す価値はある。**いまのテスター確認が終わってから。**

🔴 **着手するなら要件3.1の能力表の書き換えから始める。**
`CLAUDE.md` が「配り方・動く場所を変えるなら、要件の能力表を書き直すまでが変更」と
決めている。表を直さずに作ると、**存在しない人間を説明した要件が残る**
（この repo は一度それで欠陥を4件出している）。

---

## 2026-09-21 ライセンシーは「初日に1回、以後は何もしない」にする

**オーナーの目標:**「ライセンシーは、基本的にほぼ何もしないで進められるようにしたい」

### 覆った前提

`taking-updates.md` の案B（2026-09-11 採用）は「置いたワークフロー自身も以後の更新に
含まれるので、**1回置けば古くならない**」を根拠にしていた。**誤りだった。**

同期が運ぶのは直下の `update-workflow.yml` だけで、**実際に動いている
`.github/workflows/take-updates.yml` には届かない。**v0.8.1 でそれを踏み、
`amp-test` で手で貼り替えた。**案Bの費用は「初日に1手順」ではなく
「初日に1手順 ＋ 更新器が変わるたびに1手順」だった。**

### 決定: 案D（更新器を「薄い殻」にする）を採る（オーナー）

殻は `scripts/` のスクリプトを呼ぶだけの数行にし、中身は同期で新しくなる場所に置く。
**App も、トークンも要らない**ので、`taking-updates.md` 4節で却下した理由には触れない。

**初日の1回は消せない。**Deploy ボタンがワークフローを運べないのは Cloudflare 側の制約で、
こちらのリポジトリをどう直しても越えられない。

### 🔴 順番の制約: 案Dより先に、テスターに渡さない

手順書（4-2）・オンボードの板（`Ready.dc.html`）・その README は、どれも
「更新を受け取る口を**1回だけ**作る」と書いている。**初日の設置は本当に1回なので、
案Dが入っていれば正しい。**案Dより先に渡すと、この3か所は**黙って嘘になる**
（更新器が変わるたびに、書いていない貼り替えが要る）。

### 案E（インストーラ型）は別に判断する

**オーナーの問い:**「インストールするときに『いいですか？』と出てシステム領域にも
コピーする。それと同じでは？」

**形は同じ。**ただし Deploy ボタンの「いいですか？」は **Cloudflare のもの**で、
何の権限を求めるかは Cloudflare が決める。`workflows` を求めていないので、
OK を押しても `.github/workflows/` に書く許可は含まれない。**他社の確認画面に
1行足すことはできない。**

**この問いで新しい道が見えた。** 4節で却下したのは**恒久的に**書ける状態で、
インストーラ型は**1回使って捨てる**ので別物である。代償は「私たちがサービスを
動かし続ける」こと — この製品がこれまで避けてきた性質。**案Dの上に乗せられるので、
案Dを先に入れ、案Eはあとで判断する。**

### 実測で分かったこと

- **貼り替えは、マージから5分**（`amp-test`・2026-09-21）。「ブラウザで2分」は楽観的だった。
  読む時間を含めると5分。**コピー元と1バイトも違わず貼れていた**
- **`.github/` の変更でも、Cloudflare は配置し直す**（main への push なら中身に関係なく）。
  ただし `.github/` は Worker に入らないので、作り直された Worker は前と同じ。
  **害のない、空振りの配置**である

---

## 2026-09-21 案D は本物で動いた。そして殻には、予告された変更が1つある

v0.9.0 を出し、`amp-test` で本物の移行を通した。古い更新器で取り込み → マージ →
殻を貼る（マージから2分、コピー元と1バイトも違わず）→ **殻で Run workflow が success**
（上流を取り、差分が無いので何もせず終了）。**殻とスクリプトが本物の runner で動いた最初の1回。**

### 案D の第1版の仕様は、危険だった（記録として残す）

最初に書いた仕様は「走っているスクリプトを rsync が上書きする罠があるので、`/tmp` にコピーして
実行せよ」だった。レビューで2つとも誤りと分かった。

- **罠は起きない。**rsync は一時ファイルに書いて rename するので、実行中のプロセスは古いファイルを
  読み続けて完走する（再現で確認）
- 🔴 **そして対策が本物の事故を作るところだった。**スクリプトが自分の位置から根を求めると、
  `/tmp` では根が `/` になり、**`rsync --delete` が別の場所を消す**

**起きない罠を防ごうとして、実害のある削除を仕込むところだった。**言語を node にし、根は
`$GITHUB_WORKSPACE` から取り、rsync の前に3段で止める形にした（上流の取り置き先とリポジトリが
重なったら止まる — 根が `/` ならこれで止まる）。

### 殻に置いてはいけないもの

**殻は1文字変えると全員に貼り替えを頼む**ので、sha256 をテストで固定した（関所）。
出す前に、**古くなる事実を3つ抜いた**：除外の一覧（**既に `.git` が抜けていた**）・約束の一覧
（**既に `TAKE_UPDATES_SCRIPT` が抜けていた**）・「1分後に PR ができる」（実際は6.4分待った）。
**直せない側に、直す必要が出る事実を置いていた。**原則: **古くなりうる事実は殻に書かない。**

### 予告された変更

- **`actions/checkout@v4` は Node 20 向けで廃止予定。**いずれ `@v5` にする。**殻の変更なので
  全員に貼り替えを頼む。**そのときは CHANGELOG に理由を書き、sha256 の関所を意図して越える
- **`ubuntu-latest` は 2026-10-19 から Ubuntu 26。**殻は変わらない。runner の node が
  `engines` の 22.18 以上かを、その前に確かめる

---

## まだ決まっていないこと

要件定義 10節（未決事項 1〜19）が正。**次に手を動かす順番**は
[いまどこにいて、次に何をするか](STATUS.md)。とくにこの時期に効くもの:

| # | 事項 |
|---|---|
| 5 | 契約の決め事と法務確認。**来週の先行利用者に渡す許諾書が確定しない** |
| 19 | Amazon の対象SNSに Threads が入っているか。**5分で確定し、事実なら既定のチャネルと両立しない** |
| 13 | ステップ途中で落ちたときの二重投稿。**Cron の再試行という引き金が増えるので優先度が上がった** |
| 15 | 既定の置き場所。Cloudflare に寄せる方針は決めたが、実デプロイの検証が残っている |

---

## 2026-09-23 トップ画面から判断待ちを外す（複数アカウント前提へ）

### 決めたこと

**トップ画面は「どのアカウントが、どんな状態か」だけにする。**判断待ちの中身、
予約中の投稿、手渡しの投稿カードは**各チャンネルの画面へ移す**（実は既に在る —
`#/ventures/<id>` は最初からそのアカウントの判断待ちだけを出していた。
トップが重複して出していただけである）。

トップに残るもの：アカウントごとの状態、直近の数字（全アカウント合計）、最近の動き、
探索の提案（アカウントに属さないため）。

**1アカウントのための分岐は作らない。**1つでも複数でも同じ手順にする。

### なぜ覆したか

[コンソールの設計](../3-development/console-architecture.md) §3 は3階層を決め、
第1階層について「判断待ち、予約中の投稿。**変えない。ここが今も正しい**」と書いていた。
**これを覆す。**

- **前提が変わった。**あの記述は「毎日30秒で終わる」を守るためのもので、**1アカウントなら正しい。**
  オーナーは 2026-09-23 に「**複数アカウント前提です**」と決めた。3アカウントで実際に運用した上での判断
- **複数になると、いまの設計は逆に働く。**3アカウントぶんの判断が1本のリストに混ざる。
  オーナーの言葉：「全体的に、セクションもなく、わかりにくい」
- **手順を2つ持つと、片方が腐る。**1アカウント用と複数用を分けると、手順書・設計キャンバス・
  画面の説明がすべて2本になる。**このリポジトリは「同じ問いに2つの文書が答えていて、
  生きているのは片方」を繰り返し踏んでいる**（2026-09-20 の「現在地」4箇所など）。
  オーナーの判断：「いずれ複数アカウントになるという考えから、同じ操作手順にしたらいい」

### 条件（同時に満たすこと。どれも欠けると引き算が危険になる）

1. 🔴 **コメントアウトではなく消す。**残せば腐り、理由を知らない誰かが戻す。
   消して、戻したくなったときのためにこの記録を残す
2. 🔴 **バッジが構造物になるので、検査を付ける。**いまは判断待ちそのものがトップに出ているので
   見落としようがない。外すと**「対応が要る」と伝えるのはバッジだけ**になり、
   バッジが 0 を出せば仕事は静かに止まる。**トップのバッジの数が、実際に待っている判断の数と
   一致することを検査するテスト**を必ず付ける。
   このリポジトリには同じ形の記録がある —
   「**記録が無いことと、何も起きていないことは、画面の上で見分けがつかない**」
3. **`console-architecture.md` の「変えない」を、理由ごと書き換える。**
   UX 提案書（`console-ux-proposal.md`）の節の順番と、設計キャンバスの板
   （`Main.dc.html`・`Idle.dc.html`）も揃うまでが完了

### バッジは2種類にする（時間に縛られているものだけ強くする）

**手渡しの投稿カードだけは性質が違う。**判断待ちは「あなたが決めるまで待つ」もので、
1時間遅れても失うものは少ない。**手渡しは、枠の時刻にあなたが投稿しないと枠が過ぎる。**
機械が特定の分に人を待っている、唯一のもの。

案は3つあった：(1) 手渡しだけトップに残す、(2) 全部外してバッジを2種類にする、(3) バッジ1種類。
**(2) を採る**（2026-09-23 オーナー合意）。理由は、(1) は「トップは状態だけ」という
原則に例外を1つ作ることになり、**例外は増える**から。

バッジは少なくとも3つの事実を区別する必要がある：
**承認が要る** ／ **いま投稿する番です**（強く） ／ **失敗している**。
3つ目は `console-architecture.md` が既に「失敗だけは例外にする」として決めている —
一覧に出さないと気づくのが翌週になる、という実績がある。

### これは何を無駄にしないか

**門を「推奨」と「そのほか」に割った変更（PR #72）は、そのまま生きる。**
`renderDecision` はトップとチャンネル画面で共有されており、分割はどちらでも効く。

---

## 2026-09-24 Workers Builds の非本番ブランチのビルドは残す

### 決めたこと

ライセンシーの控えで、Cloudflare Workers Builds が `platform-update` ブランチも
ビルドする設定を**そのままにする**（2026-09-24 オーナー判断）。

### 発端

オーナーが Version History に**更新1回につき2行**出ることに気づき、
「main だけにしたのでは」と訊いた。**リポジトリにその設定の記録がゼロだった** —
`docs/` にも `wrangler.jsonc` にも無く、親もオーナーも確かめようがなかった。

### 実態（Cloudflare の設定画面で確認）

```
Production branch   main
Deploy command      npm run deploy               ← main のとき。本番に出る
Version command     npx wrangler versions upload ← それ以外。上げるだけ、配らない
```

**`platform-update` の行は本番に出ていない。**`wrangler versions upload` は版を
アップロードするだけで配置しない。

🔴 **つまり「PR を見る前に更新が本番へ反映される」は起きていない。**
更新を PR で確認してからマージする、という設計は効いている。**これが確認したかった点。**

### なぜ切らないか

`platform-update` のビルドは遊んでいない。**届いた更新がそもそもビルドできるかを、
マージする前に確かめている。**壊れた更新が届けば PR の段階で分かる —
マージして本番が落ちてからではなく。

切れば Version History は半分になり、ビルド時間も減る。
**ノイズと引き換えに事前チェックを捨てる取引**なので、残す。

### 未確認

**PR にビルドの結果（チェック）が表示されるかを、実物で確かめていない。**
表示されないなら事前チェックとして人に届いておらず、残す理由が弱くなる。
amp-test の PR を1つ開けば分かる。

### なぜ記録するか

**これはライセンシー全員に効く。**Deploy ボタンが Workers Builds を設定するので、
既定がそのまま全員の設定になる。記録が無かったせいで、オーナーも親も
「そういえばどうしたんだっけ」から始めることになった。

---

## 2026-09-24 空の行は「何も無い」を伝えない。行ごとに事実を言う

### 決めたこと

**状態が「承認が要る」「いま投稿する番です」「失敗しています」のどれでもないとき、
行は空にせず、次のうちどれかを言う。**

- 稼働中で、今日のサイクルが今日の日付で記録されている → 「今日は完了しました」＋
  「次回 {次のサイクルの時刻}」（`cadence.cycleStartsAt` から `nextLocalTime` で求めた
  実際の日時。`when.at()` 経由で、時計の名前を必ず添える）
- 稼働中で、今日のサイクルがまだ無い（前回が今日より前の日付、または一度も無い）→
  「今日はまだ動いていません」。**次はいつかを言わない** — 本当に止まっているとき
  嘘の安心を言わないため
- 稼働中で、今日のサイクルが `running`（今日は動いたが終わっていない）→ 「実行中です」。
  「完了しました」は嘘になる
- 非アクティブ（`amp venture deactivate` 相当）→ 「非アクティブです」
- 停止中（緊急停止）→ 「停止しています」。`--failed` と同じ見た目の帯（`--stopped`）を
  新設した — 「これは止めた側の状態で、失敗ではない」という重みの違いを保ちつつ、
  静かな行と取り違えないよう危険色は共有する
- 設定で `active: false`（一度も動かしていない）→ 「設定で無効です」

**非アクティブ・停止・設定無効は、待ちバッジ（承認が要る／いま投稿する番です／失敗しています）
より先に見る。**`closeOpenGates`（`orchestrator.ts`）は非アクティブにした瞬間の
サイクルを `failed`（コード `venture.deactivated`）のまま残し、次のサイクルが動かない限り
永久にそれが「直近のサイクル」になる。この行が `lastCycle.status === "failed"` だけを見ていたら、
**自分から止めたアカウントが「失敗しています」と表示され続ける。**

### 発端

`amp-test` の本番トップ画面を見ると、判断待ちが無い3アカウントの行が
`<span class="status-facts"></span>` — 空。`console-architecture.md` が名指しした欠陥
（「記録が無いことと、何も起きていないことは、画面の上で見分けがつかない」）を
バッジ1種類ぶんは塞いだつもりが（2026-09-23 決定の条件2）、**バッジが1つも立たない日**の
ほうは塞がれていなかった。**週の大半がこの日である。**

`outdoor` はさらに `deactivated` だったが、行は他の2つと見分けがつかなかった。

### なぜ `console-ux-proposal.md` §5.2 の絵をそのまま実装しなかったか

§5.2 は「今日」ページ全体の休みカード（会社全体で1枚、「3アカウントとも最後まで
終わりました」）を描いていた。**採らなかった。**

- あの絵はアカウントが増えるほど1枚の文で嘘に近づく。3つのうち1つだけ今日動いて
  いない日、「3アカウントとも」は言えない。アカウントごとの真実を1枚の合成文に
  畳むと、畳んだ側が正しさの責任を持つことになる
- **行はすでにアカウントごとの真実の単位**（2026-09-23 決定がそう決めている）。
  同じ単位のまま「待っているか」の次に「動いたか」を足すほうが、新しい合成の階層を
  持ち込まずに済む
- 「次はいつ」を全社1個で言うと、アカウントごとに `cadence.cycleStartsAt` と
  `timezone` が違う場合に対応できない。行ごとなら最初から対応している

§5.2 が挙げた2つの事実（次はいつか／今日どうだったか）と、データが既にある
という指摘はそのまま使った。**器を変えただけである。**

### 検査

`test/console.test.ts` に4本追加。うち3本は `node scripts/mutate.ts` で
赤くなることを確認済み（バグを実際に戻し、テストが落ちるのを見てから戻した）：

- 稼働中・完了・次回時刻を言う行
- 稼働中・今日はまだ動いていないと言う行（`ranToday` のガードを外すと、
  一度も動いていないアカウントが「今日は完了しました」と表示されるようになり、
  この記録の欠陥をそのまま再現した）
- 非アクティブな行が、閉じたゲートの残す `failed` サイクルより優先して
  「非アクティブです」を言う（`failed` の判定に `row.state === "active"` の
  ガードを外すと再現した）
- 停止中の行が「停止しています」を言う

### 触れたもの

- `src/console/router.ts` の `buildState`：行ごとに `ranToday`（今日の日付と
  `lastCycle.date` の一致、アカウント自身のタイムゾーンで）と `nextCycleAt`
  （`cadence.cycleStartsAt` を `nextLocalTime` で次の実時刻に解決し `when.at()` で
  整形）を追加。他の全フィールドはそのまま
- `src/console/page/client/venture.ts` の `renderStatusRow`：新設の `idleFacts()`
  に空文字列だった分岐を委譲。`failed` の判定に `row.state === "active"` を足した
  （上記「非アクティブが優先」の実装）
- `src/console/messages.ts`：`status.ranToday` / `status.nextCycle` /
  `status.cycleRunning` / `status.notRunYet` / `status.rowDeactivated` /
  `status.rowStopped` / `status.rowConfigInactive`（ja/en 対）
- `src/console/page/style.ts` と `docs/_proposed/design/Idle.dc.html`：
  `.status-badge--stopped`（`--failed` と同じ見た目、別クラス）
- `docs/_proposed/design/README.md`：「動いているか止まっているかは全社の数字と
  最近の動きで示す」という 2026-09-23 時点の記述を、「行そのものが言う」に更新
  （下記「訂正」）

### 訂正

`docs/_proposed/design/README.md` は 2026-09-23 の書き直しで
「動いているのか止まっているのかを、**会社ぜんたいの数字と最近の動きで**示す」と書いていた。
**これは正確ではなかった。**個々のアカウントが動いたかどうかは、全社合計にも
最近の動き（直近12件に切り詰められる）にも確実には出ない。今回、行そのものに
言わせることにしたので、その記述を書き換えた。

---

## 2026-09-25 画面が5秒待っていた。キャッシュのキーが3回続けて1階層下へ逃げた

### 何が起きていたか

オーナーが「遅い・追従していない」と言い、**親が実機（amp-test・D1・3アカウント）で測った**：

```
/api/state              5,295ms     /api/ventures/<id>  3,417ms
→ 切り替えの実待ち 約8.7秒
```

**2回目が速くならない**のが手がかりだった。30秒の記憶があるのに効いていない。

### 同じ欠陥を、3回続けて「直した」

| 版 | キー | 結果 |
|---|---|---|
| 〜v0.12.0 | `WeakMap<Runtime>` | Worker はリクエストごとに runtime を作る → **当たらない** |
| v0.12.1 | `WeakMap<D1Database>`（`env.DB`） | Cloudflare は `env` をリクエストごとに渡す → **当たらない** |
| v0.12.2 | **モジュール直下の変数1つ** | 渡されたものを見ない → 当たる |

**毎回「直した」と思い、毎回まだ当たっていなかった。**そして毎回、**手元では効くので気づけない。**
`sql-store.ts` のコメントは「ドライバが生きている限り1回」と**正しく書いていた** —
呼ぶ側がその前提を壊していただけである。

🔴 **v0.12.1 のテストは通っていた。**「**まったく同じオブジェクトを2回渡して**、キャッシュは効く」
と検査していた。それは「`WeakMap` は渡し返されたキーを見つけられる」を証明しただけで、
**Workers が本当に同じものを2回渡すのか**——問いの全部——を一度も再現していなかった。
いまのテストは**同じ DB を指す別々のオブジェクト**を渡す。

### 直列だったものも並行にした

スキーマ確認（毎リクエスト7往復）・`gather()` のアカウント順次読み・`buildPortfolio` の
アカウントごとのループ・`computePerformance` の2波・画面側の2つの取得。
**合計で約30往復が直列**だった。

### 実測（親がすべて実機で測った）

| | v0.12.0 | v0.12.1 | v0.12.2 |
|---|---|---|---|
| `/api/state` | 5,295ms | 3,600ms | **1,722ms** |
| `/api/ventures/<id>` | 3,417ms | 2,280ms | **1,000ms** |
| 切り替えの実待ち | **8.7秒** | 3.6秒 | **約1.7秒** |

**まだサブ秒ではない。**床が 1.72秒で安定しており、残りが記憶の対象外の処理なのか、
記憶がまだ当たっていないのか、**この数字だけでは区別できない。**

### ここから引く教訓

**「手元で効く」は、Worker で効く証拠にならない。**このセッションだけで同じ帯域を4回踏んだ
（wrangler の `__name` 注入・設定検証がビルドを止めない・`/healthz` が版を返さない・このキャッシュ3連）。
**Worker で効くかを確かめる唯一の方法は、デプロイして測ることである。**

---

## 2026-09-25 実機で確かめた（親がブラウザで直接）

オーナーの指摘「あなたブラウザ操作できるんだから、動作させて、動きとか見て」を受けて、
**親が実際の画面と API を自分で見るようにした。**それまでは伝言で往復していた。

**その回で分かったこと：**

- **「アウトドア(3) が完了しない」は不具合ではなかった。**`published: 1` で完了していた。
  **古い画面がそう見せていただけ。**この欠陥は表示を誤らせるだけでなく、
  **診断そのものを狂わせていた**（親も2回誤診している）
- **遅さが「体感」ではなく 5,295ms という数字になり**、原因が特定できた
- **状態の行が完全に空**であることが分かった（DOM を読んで発見。`status-facts` が空文字）

**v0.12.2 の検証（すべて実機・実データ）：**

| | 結果 |
|---|---|
| バッジの数 | 🟢 3アカウントとも API の実データと一致 |
| 移動時に前のアカウントが残るか | 🟢 **60ms 後には空**。見えている瞬間が無い |
| 速度 | 🟢 8.7秒 → 約1.7秒 |
| 表の幅 | 🟡 828px の他節に対し **1,106px**。左右139pxずつはみ出す |

**表の幅は「1（他と揃える）」をオーナーが選択**（2026-09-25）。
中途半端な139pxが「意図」ではなく「事故」に見えるため。
## 2026-09-25 全アカウント表の幅を、他の節に合わせる（横スクロールを許容）

### 決めたこと

**`#portfolio-section`（全アカウント）に、自分だけの幅を持たせない。** main と同じ
828px（`main`の`max-width: 860px`から左右16pxずつの padding を引いた幅）で、
状態の一覧・探索の提案・数字・最近の動きと揃える。十一列が収まらないときは
`.table-wrap`の`overflow-x: auto`で表の中だけ横に流す。オーナーの選択
（「横幅が他のところと合っておらず、表がはみ出た感じに見える」への回答として、
両立しない2つ——ページ全体との幅の一致と、表が常に収まりきること——のうち前者）。

### 発端

デプロイ済みページ（v0.12.2、ビューポート1554px）を実測：他の4節は828px、
全アカウントだけ1106px（tableWidth。左右に139pxずつはみ出す）。v0.12.1で
ハードコードの1240pxをtableWidthに揃えた改善はすでに入っていたが、
「揃った」のは表と自分の`min-width`の間だけで、ページの他の部分とは揃っていなかった。

### 変わったこと・変わらなかったこと

- **stackBelow（1138px = tableWidth + PAGE_GUTTER）は変えていない。** これは
  *ウィンドウ幅*のブレークポイントで、セクション自身の描画幅（今回main基準の
  828pxに固定した）とは独立している。ウィンドウが1138px以上ある限り、表は
  カード化されず実テーブルのまま描かれる——実測した1554pxのような通常のデスクトップ
  サイズで、勝手にカード表示へ落ちることはない。確認ずみ
- **列のドラッグで保存された幅が表をさらに広げる余地は、今回の変更の前からあった。**
  `.table-wrap`の`overflow-x: auto`がその逃げ場で、セクション自身の幅を変えても
  この仕組みは変わらない
- **横スクロールは`.table-wrap`の中だけで機能し、列が見えなくなるわけではない。**
  ただし正直に言うと：828px内に収まるのは概ね11列中7列（名前・状態・直近サイクル・
  投稿・中央値・クリック・成果）までで、確定報酬・型・計測・そして各行唯一の操作
  である「開く」は、通常のデスクトップ幅でも初期状態では画面外になる。今回の
  ブリーフで**オーナーが明示的に許容した**トレードオフだが、「開く」ボタンが
  毎回スクロールしないと見えないのは軽くない代償なので、次に触るときのために
  ここに残す

### 「列の幅をもとに戻す」を、常設から条件表示へ（`console-ux-proposal.md` §6.3）

**まだ未着手**とされていた提案を実装した。列をドラッグしたことがない限り無意味な
操作なので、ボタンはmarkup上`hidden`で出荷し、`wireColumnResize`が保存された幅の
有無（`Object.keys(widths).length`）で表示・非表示を切り替える：ドラッグ完了で
表示、「もとに戻す」クリックで再び非表示。

### 検査

`test/console.test.ts`に1本追加、2本を書き換え。**CSSの幅自体は
実測できない**（`page-harness.ts`自身が明記する通り、レイアウトもポインタも
無いスタブ）ため、幅の変更は「`#portfolio-section`に幅を持つCSSルールが
無いこと」と「`.table-wrap`が`overflow-x: auto`を保つこと」を回帰の対象にした。
表示切り替えのロジックは3行とも`node scripts/mutate.ts`で赤を確認済み。
グリップのドラッグ操作自体（ポインタイベント）は`page-harness.ts`の既知の
守備範囲外で、今回もその外のまま——拡張は今回のスコープ外と判断した。

### 触れたもの

`src/console/page/style.ts`（`#portfolio-section`のルールを削除、比較コメントを
書き直し、`PAGE_GUTTER`の未使用importを削除）、
`src/console/page/client/helpers.ts`（`wireColumnResize`に表示切り替えを追加）、
`src/console/page/client/today.ts`（ボタンに`hidden`属性）、`test/console.test.ts`。
`docs/_proposed/design/`はこのデスクトップ表を描いていない（全キャンバスが390px幅の
携帯フレームで、全アカウントの絵はカード1枚のモバイル表示のみ）ため、再シードは無し。

## 2026-09-25 最近の動きを、監査ログではなくニュースにする

### 決めたこと

**最近の動きを、監査ログの生の記録から合成した「起きたこと」の一覧に変える。**
オーナーの指摘：「最近の動きは、システム的なものは表示せず、フォロワーからのクリック、
成果や報酬の向上など、ライセンシーに意味のある最近の動きを表示しましょう。そうしないと、
最近の動きがただのログみたいになっちゃう」。実際そうなっていた——`src/console/router.ts`
の `activity` は `store.audit.recent(12)` をほぼそのまま整形していただけで、12件中8件が
未訳の英語、うち1行は下書きの内部id（`drf_54713830758a4727ac54`）をそのまま出していた
（[STATUS.md](STATUS.md)「画面を操作して見つけた3件」の1番目、オーナーが実機で発見）。

**載せる種類（ホワイトリスト）：**

- **クリックが入った**（`store.clicks`）——オーナーが最初に名指ししたもの
- **成果・確定報酬**（`store.conversions`）——`pending`は「成果が入りました（確定前）」、
  `approved`は「確定報酬が入りました」、`rejected`は出さない（後述）。金額は
  `payoutFor`（`src/affiliate/attribution.ts`）を再利用し、通貨をまたいで合算しない
  ——1件＝1通貨のイベントなので、合算という操作自体が構造上起こらない
- **投稿が公開された**（`post.published`）——自動で出た分と、本人が手で投稿した分
  （下の「あなたが投稿する番です」の後に「投稿しました」を押したもの）の両方が
  `post.published` として監査ログに残る。区別する理由がないので同じ文言にした
- **あなたが投稿する番です**（`post.handed_over`）——チャネルが自分で投稿できず、
  人の手を待っている、という既存の訳文（`today.activityHandedOver`）をそのまま使った。
  アカウントの画面にも同じ内容の常設カードがあるが、こちらは「いつ渡されたか」という
  出来事としての記録で、カードは「いま何を待っているか」という現在の状態——重複ではなく
  別の質問に答えている
- **人が決めた**（`decision.resolved`・`venture.deactivated`・`venture.activated`）
  ——ブリーフが挙げた「承認・見送り・停止」のうち「停止」は `venture.deactivated`／
  `venture.activated` で、この節を書くまで見落としていた。「1 of 3 approved」は
  「3件のうち1件を承認しました」に、全部見送ったときは「全て見送りました」に分けた
  ——ブリーフが名指しした「skipped」は内容として存在していたが、元の英語は
  それを表現していなかった。`decision.closed`（アカウントを止めたので判断が閉じた）も
  ここに含めた——人が止めたことの結果だが、`decision.expired`（誰も答えないまま
  日が変わった——人は何も決めていない）とは別物なので、既存の訳文をそのまま
  流用しつつ両者を混ぜなかった
- **判断が閉じた・答えのないまま日が変わった**（`decision.expired`・上記の
  `decision.closed`）——「人が決めた」ではなく「決まらなかった」だが、
  console-architecture.md が最初にこの節を書いたときすでに訳文（`today.activityExpired`・
  `today.activityClosed`）を持っていたので、そのまま `describeAuditEvent` に移した。
  30秒の判断が静かに消える日、という「最近の動き」がもともと守っていた約束の一部
- **全体の停止を解除した**（`platform.resumed`）——既存の訳文をそのまま移した。
  非常停止という、日常ではまず起きないが起きたときは重い操作なので、頻度が低くても外さない
- **🔴 失敗**（必須）——`cycle.failed` は `FAILURE_SUMMARIES` と `CYCLE_STEP_LABELS`
  から組み立てる（元からこの節が決めていた通り）。`post.link_drop_failed`
  （投稿の下にアフィリエイトリンクが結局つかなかった失敗）も同じ扱いに加えた——
  CLAUDE.md が名指しする「リンクを落とし続けた」欠陥の再発形で、収益に直結するのに
  元の実装ではこの種別も他の`role.*`と同じ扱いで埋もれていた

**落としたもの：** `role.*.completed`・`role.*.dropped_*`・`comment.compliance`
（六つのロールが自分の手順を語る行——元の欠陥そのもの）、`decision.opened`
（同じページの状態行が判断待ちを既に出している）、`decision.auto_resolved`
（`autonomy: auto` で機械が決めたものを「人が決めた」と言うのは嘘になる）、
`role.scout.*`（探索の暦は `lastScoutAt` が読む。画面で追う日課ではない）。

### 空のとき

**「まだ記録がありません」だけでは足りない。** クリック・成果・判断のどれも起きていない
日は普通にある（3アカウントでまだ集客できていない、など）——フィルタを強くした分だけ、
「記録が取れていない」と「本当に何も起きていない」が画面の上でまた見分けられなくなる。
`直近サイクル`欄で一度直したのと同じ罠（v0.12.2、状態行の空文字問題）。
**今の文言：**「まだ記録がありません。クリック・成果・投稿・判断など、意味のある
動きがあるとここに出ます。」——この欄が何を待っているかを名指しすることで、
「動いていない」と「壊れている」を区別できるようにした。

### 今回あえて出さなかったもの

- **`proposal.accepted` / `proposal.dismissed`**——探索の提案（週1回）の採用・見送りも
  「人が決めた」の一種。ただしニッチ名は監査データの `data` に無く（`proposalId` しか
  無い）、`store.proposals.get()` への追加読み出しが要る。件数が少なく（週1回・
  `exploration.enabled` が既定オフ）、優先度を下げた。次にこの節を触るときの入口
- **`post.failed` / `post.deferred`**——チャネル側の投稿失敗。コードの語彙
  （`channel.*`・ネットワークの都合）が `FAILURE_SUMMARIES` と別物で、素の英語を
  出さずに翻訳する表を今は持っていない。「APIのメッセージを切り詰めない」という
  この文書自身の制約を守ると、今は固定文すら用意できていない——**「失敗はどちらにも
  出す」という原則をこの2つには適用できていない、という認識付きの未対応**であって、
  見落としではない
- **`role.analyze.unattributed`**——どの投稿のものか分からない成果。データ品質の
  警告で、日々読む画面よりアカウントの画面（原因を調べて直す場所）向きと判断した
- **`decision.rejected`（conversion の `rejected`）**——ネットワークが「これは
  カウントしない」と言っているだけで、今日オーナーが対応すべき新情報ではない。
  毎回出すとこの欄が「進捗」ではなく「苦情ログ」に読める

### 検査

`test/console.test.ts` に2本追加。1本（「最近の動き reads like news, not the audit log
it is built from」）は実機に近い1日ぶんのデータ（クリック1・成果2（pending/approved）・
rejected1・decision.resolved・post.published・cycle.failed、加えて元のバグ報告と
同じ文面の `role.*` ノイズ5行）を`openPage`で描画し、内部id・内部ステップ名の英語・
未訳の英語が画面に出ないこと、6種の必須行が日本語で出ることを検査。もう1本
（「最近の動き says what it is waiting for...」）は空表示の文言を検査。

`node scripts/mutate.ts` を3回実行、いずれも変異でテストが落ち、復元後は
バイト単位で元に戻ることを確認した：

1. `describeAuditEvent` の `case "cycle.failed":` を無効な種別名に変異
   → 「reads like news」と、既存の「the activity feed says the day died」の
   **両方**が赤くなった（失敗を落とすと2本とも落ちる、という二重の保証）
2. `default: return undefined;`（ホワイトリストの外を落とす分岐）を、素通りで
   `summary`をそのまま出す分岐に変異 → 「reads like news」が赤くなった
   （ノイズが実際に画面へ戻ってくることを確認）
3. `today.activityEmpty` の ja 文言を元の「まだ記録がありません。」に変異
   → 「says what it is waiting for」が赤くなった

`arch -arm64 npm run check` は**615件、緑**（追加2本を含む）。

### 触れたもの

`src/console/router.ts`（`buildActivityFeed`・`describeAuditEvent`・`subjectFor`を追加、
`activity` の構築をこれに置き換え）、`src/console/messages.ts`（`activity.*`キーを追加、
`today.activityEmpty`の文言を変更）、`src/console/page/client/today.ts`（`activityText()`を
削除し、サーバーが組んだ`entry.text`をそのまま描画するだけにした）、`test/console.test.ts`。

`docs/_proposed/design/Idle.dc.html`（最近の動きの3行を、役割名を語る旧い絵から
クリック・確定報酬・人の判断・失敗の4行に更新）、`docs/_proposed/design/canvas.json`
（`note-idle`に3つ目の訂正段落を追加）、`npm run build:canvas`を再実行。
`Timeline.dc.html`にも同じ見出し「最近の動き」があるが、これは別の画面
（`#/ventures/<id>`の日別履歴を選ぶ絵）で、`src/console/timeline.ts`が読むデータも
別物のため今回は触っていない——同じ日本語見出しが2つの異なる画面を指している
ことに気づいたので、次にこの領域を触る人のために書いておく。

### 🔴 訂正（2026-09-25、レビューで発覚）——`default: return undefined` が失敗を消していた

**上の「今回あえて出さなかったもの」の `post.failed` / `post.deferred` は、
「未対応」ではなく「不可視」になっていた。** `describeAuditEvent` の `default` 節が
無条件に `undefined` を返すため、専用の `case` を持たない種別は――失敗かどうかに
関係なく――画面から消えていた。この変更の前は素の英語のまま出ていたので、
**直す前より悪い方向に直っていた。** 表示のさせ方を知らないことと、
何も起きなかったことにするのは別問題であり、後者は console-architecture.md が
名指しする「2日ぶん死んでいる間ずっと『まだ記録がありません』」と同じ形の欠陥である
（記録は監査ログに残っている点だけが違う——見える場所に無いのは同じ）。

**直した内容。**

1. `src/console/router.ts` に `FAILURE_EVENT_TYPES`（失敗を意味する監査イベント種別の
   集合）を追加。`describeAuditEvent` の `default` 節は、この集合に入っている種別
   だけ、専用の訳が無くても固定のフォールバック文を返すようにした：
   「うまくいかなかったことがあります。詳しくは記録にあります。」
   （en: “Something didn't work. See the record for details.”）。
   具体的なことは言わない――言えることが無いのに言ったふりをする方が悪いため。
   `post.failed`・`post.deferred`・`role.scout.failed` は今このフォールバックで
   拾われる
2. `cycle.retry_abandoned`（`scheduler/tick.ts` の `CYCLE_ABANDONED_EVENT`。
   「何時間もリトライして、それでも動かず、あきらめた」日）に専用の `case` を追加した。
   これは調査で見つけた**もう1つの見落とし**——`tick.ts` 自身のコメントが
   「Read by the console's activity feed」と書いていたのに、実装のどちらの版も
   読んでいなかった。`cycle.failure` が残っていれば `cycle.failed` と同じ文言
   （`FAILURE_SUMMARIES` 経由）、無ければ「{day} はやり直しをあきらめました」
3. `FAILURE_EVENT_TYPES` を機械的に裏取りするテストを追加（下記）

**「失敗を意味する」の網羅は、機械的な走査だけでは閉じない。** `test/console.test.ts`
の `scanAuditEventTypes()` は `src/roles/`・`src/kernel/`・`src/scheduler/tick.ts` の
`type: "..."`（三項演算子越しも含む）と、`note(...)` 呼び出し（`context.note(type, ...)`
と、`venture-switch.ts` だけが持つ `note(request, type, ...)` という別の引数順）を
両方たどり、定数参照（`CYCLE_ABANDONED_EVENT` 等）も自分の宣言まで追って解決する。
これで30種別中30種別を機械的に見つける。**見つからないのは1つだけ：**
`kernel/exploration.ts` の `` `proposal.${resolution.status}` `` ――値が
`ProposalStatus` という型に依存しており、ソースのどこにも完成した文字列
「proposal.accepted」が存在しないため、走査のしようがない。この2値
（`proposal.accepted`／`proposal.dismissed`、どちらも失敗ではない）はテストに
手で足し、なぜ足したかを同じ場所に書いた。**走査で見つけられないことを
黙って見落とすのではなく、見つけられないと書いたうえで手で埋めた、という違い。**

テストは2本。1本目は「走査で見つかった種別＋この2値」が「`FAILURE_EVENT_TYPES`＋
非失敗として手で分類した種別」と完全に一致すること（`pageSourceFiles()` と同じ
双方向の一致）――未分類の新種別が増えても、分類済みなのに実体が無くなった
種別が残っても、どちらも落ちる。2本目は `FAILURE_EVENT_TYPES` の全種別について、
`describeAuditEvent` が実際に何か（`kind: "failure"`、太字、空でない文面、
内部idを含まない）を返すことを検査する。

`node scripts/mutate.ts` で2か所を確認、いずれも復元は元とバイト単位で一致：

- `if (FAILURE_EVENT_TYPES.has(event.type))` を `if (false)` に変異
  → 「every failure-shaped audit event type produces a visible line」が赤化
  （フォールバックを外すと、レビューが指摘した defect がそのまま再現される）
- `FAILURE_EVENT_TYPES` から `"role.scout.failed"` を削除する変異
  → 「every audit event type ... is classified」が赤化
  （分類し忘れた新種別が増えても、今度は気づける）

`arch -arm64 npm run check` は**617件、緑**（追加2本を含む）。

触れたもの：`src/console/router.ts`（`FAILURE_EVENT_TYPES`・`CYCLE_ABANDONED_EVENT`の
`case`・`default`節のフォールバック、`describeAuditEvent`をテストのため export）、
`src/console/messages.ts`（`activity.dayAbandoned`・`activity.unknownFailure`）、
`test/console.test.ts`（`scanAuditEventTypes()`と2本のテスト）。

### 🔴 訂正（2026-09-25、`/code-review` で発覚）——クリックと成果の読み出しが無限に重くなる形だった

**`buildActivityFeed` の `store.clicks.all()` / `store.conversions.all()` は、
毎回まるごとのスキャンで、しかも `/api/state` の30秒ごとのポーリング全部で走っていた。**
`AuditLog.recent(40)` は上限つきなのに、この2つだけ無制限——ライセンシー自身の実績
（クリック・成果の総数）が増えるほど重くなる形で、しかもポーリングのたびに払う。
この日の前半でエンドポイント全体を5.3秒→1.7秒に縮めた作業と、原因の形がまったく同じ。

**まず境界のある読み出しを探した。** `Collection<T>`（clicks・conversionsもこれ）には
`AuditLog.recent()` に相当する「直近N件」が無い——並び順を持たず、JSON/メモリ/D1の
どのアダプタも「新しい順にN件」を返す実装を持っていない。ポートを直すのは
clicks・conversions だけの都合で3つのアダプタ全部に手を入れることになり、
今回の範囲を超えると判断し、キャッシュを選んだ。

**`forgetPortfolio` には繋がない。** クリックや成果が最大30秒遅れて出ることは
欠陥ではない——このページ自体が30秒ごとに描き直す前提で、最近の動きは
「何が起きたか」を映すビューであって、何かがそれを待つ門ではない。
自然に失効させることで、6つ目の呼び出し場所に7つ目のキャッシュの破棄を
覚えさせる必要が無くなる。

**直した内容。** `memoisedClicksAndConversions`（`WeakMap<Store, {...}>`、TTL
`ACTIVITY_EVENT_MEMO_MS`＝30秒、`memoisedPortfolio`と同じ考え方——`Store` は
`StoreRegistry.for()`がアカウントごとに使い回す同一オブジェクトなので、
そこに直接キーを打てる）を追加し、`buildActivityFeed` はこれ経由で読む。
あわせて、クリック・成果それぞれの `store.links.get()` 解決を `for` の直列 `await` から
`Promise.all` に変えた——このPRのすぐ隣のコード（`upcomingRawPromise` 等）が
すでにそうしている形に揃えただけ。

**検査は時間ではなく回数で。** `test/console.test.ts` に
`memoisedClicksAndConversions` を直接呼ぶ単体テストを追加。**HTTP経由
（`/api/state`）では書かなかった**——`buildState` は `memoisedPortfolio` も呼び、
そちらも同じ2つのコレクションを独立した30秒TTLで読んでいるため、HTTP越しに
呼び出し回数を数えると2つのキャッシュが混ざり、**この関数自体が壊れていても
ポートフォリオ側のTTLが同じタイミングで切れて、赤くならずに緑を装うことがある**
（実際に`node scripts/mutate.ts`で踏んだ——下記）。関数を直接呼び、`nowMs`を
ただの引数として渡すことで、時計もHTTPも要らず、この曖昧さごと消した。

`node scripts/mutate.ts` で2方向を確認：

- TTL判定を `if (false) return cached;` に変異（＝二度とキャッシュを使わない）
  → 直接呼び出しのテストが赤化。**HTTP経由の初版のテストでも赤化した**
- TTL判定を `if (cached) return cached;` に変異（＝二度と失効しない）
  → 直接呼び出しのテストは赤化。**HTTP経由の初版のテストは緑のまま
  （`mutate.ts` が exit 1 = DECORATION と報告）**——`memoisedPortfolio` の
  独立した失効が総数を動かし続けたため。**この失敗自体が、HTTP越しに数える
  設計が誤りだったという証拠になった。**

いずれの変異も復元は元とバイト単位で一致。`arch -arm64 npm run check` は
**618件、緑**。

触れたもの（追加分）：`src/console/router.ts`（`ACTIVITY_EVENT_MEMO_MS`・
`activityEventMemo`・`memoisedClicksAndConversions`、`buildActivityFeed`内の
クリック・成果ループを`Promise.all`化、両方をテストのため export）、
`test/console.test.ts`（単体テスト1本、HTTP経由の初版を置き換え）。

---

## 2026-09-25 サブスクの件、訂正（オーナー指摘）

2026-09-20 の却下理由②「契約違反がライセンシーに相続される」は、**前提が間違っていた。**

**このプラットフォームは Claude へのアクセスを転売していない。**ライセンシーは
ソフトウェアをライセンスされるだけで、**自分の Claude 契約を、自分のアフィリエイト業務のために
自分で使う。**「会計ソフトを買った人が、自分の ChatGPT で自分用のメモを書く」のと同じ形で、
Anthropic の規約が問題にする「第三者向けの商用サービスの裏で1つの契約を使い回す」形には
そもそも当たらない。

**加えて、Cowork には「予約実行」「バックグラウンドでのPC操作」が公式機能としてある**
（2026年9月2日出荷）。「サブスクは人が見ているその場のセッションにしか紐づかない」という
前提も、もう正確ではない。

**成立しうる形（設定でオン/オフを切り替える案）：**
- オン（既定）：いまどおり Cron が Anthropic API を叩く。課金あり
- オフ：Cron は執筆を待つだけ。**ライセンシー自身の Cowork の予約実行（か手動起動）**が
  プラットフォームの窓口を読みに来て、下書きを置いていく。API費用ゼロ

**残る技術的な溝：**プラットフォームがライセンシーの Cowork を呼ぶ向きの API は無い。
**逆向き**（ライセンシーの Cowork がプラットフォームの窓口を呼ぶ）なら作れるが、
「外からの答え待ち」という新しいサイクルの状態と、書き込み用の窓口が要る。
下書きの検品（`policy.ts`）は出所を問わないので、そこは崩れない。

**却下していた前提の②は取り下げる。①（要件3.1・端末を持たない、を捨てることになる）は残る**
——これは変わらず、ライセンシー向けの既定機能にはできない理由として立つ。

**まだ実装しない。**次に手を付けるときの仕様化の材料として記録するだけ。

---

## 2026-09-25 「案件なしの投稿」戦略は、記録はできているが検証はできていない

### 発端（オーナーの問い）

「アフィリエイト記事はリンクがあって初めて成立するのに、いまはチャンネルのテーマから
案件と無関係に投稿文を自動生成している。案件に合わないものを作っても意味がないのでは。
それとも、案件が無くても投稿して読者を獲得し、たまにアフィリエイトを流す戦略なのか」

### 答え：意図した戦略。プロンプトに明文化されている

`prompts/planner.system.md`：

> **At most half carrying an affiliate offer.** アカウントが毎回売り込むと、
> 読まれなくなる。**案件の無い投稿が、案件のある投稿を成立させる資格を稼ぐ。**
>
> **Only attach an offer when the post would have been worth writing anyway.**
> 正直に書いて自然に商品へ繋がらないなら `offerId` は空にする。無関係な投稿に
> 無理やり案件を付けるのは、読者を失う最速の方法——分析担当が1週間以内に
> 数字で気づく。

オーナーの理解（読者獲得 → たまに流す）は正しい。

### ただし、この配分に根拠は無い

「半分以下」という数字はプロンプトの指示であって、**実績で検証されたものではない。**
`analyst.ts`（型を育てる・退場させる役割）に、「案件なし投稿を挟んだ直後の、
案件付き投稿のクリック率」を測る仕組みは無い。要件にもこの配分の根拠は書かれていない。

### 記録はできている。分析ができていないだけ

`Idea`・`Draft`・`ScheduledPost` はすべて `offerId?`（無ければ空）を持ち、
投稿ごとのエンゲージメント・クリック・成果も別に記録されている
（`src/core/types.ts`）。**「案件が付いていたか」と「どう伸びたか」は、
両方ともすでに1件ずつ保存されている。**

**足りないのは記録ではなく、この2つを突き合わせて見る計算・画面。**
具体的には：①案件なし投稿を挟んだ直後の、案件付き投稿のクリック率
②案件なし投稿だけの実績の一覧。どちらも `analyst.ts` に無い。

### いま作らない理由

3アカウントとも実績が少なすぎる（クリック0件のものが多い）。いま分析の仕組みを
作っても「データ不足」としか出ない。**材料が揃ってから着手する。**

### 宿題として残す

実績が溜まったら、上記2つの分析を `analyst.ts` に足し、この配分戦略が
実際に効いているかを確かめる。それまでは `at most half` の数字を、
検証済みの結論として扱わない。


---

## 2026-09-25 見映えは「配色の刷新」ではなく「使われていない配色を使う」で進める

### 発端

オーナー：「全体的に見映えがダサイ。デスクトップでもそうだし、モバイルで見ても同じ感じに
なるのでは」。SNS分析・複数チャンネル管理のサイト（Buffer・Metricool）を実際に見て、
提案書 [visual-polish-proposal.md](../3-development/visual-polish-proposal.md) を書かせた。

### 決めたこと・分かったこと

- **配色は破綻していない。使われていないだけ。**`--accent`（緑）が意味を持って効いている
  場所（手渡しカードの枠・開示チップ）はよく効いている。見出し・数字・ラベルが全部
  黒か灰色に沈んでいる。**直すのは選び方ではなく使い方**——配色の全面刷新は採らない
- 🔴 **`--accent`（緑）を「良い数字」の演出に流用しない。**この製品では既に
  「あなたが動かないと起きないもの」という意味で運用されている。同じ緑を好調の知らせにも
  使うと、緑を見るたびに「押すべきか、ただの好調か」を考えさせる
- **実測で見つけた本物の欠陥：**全アカウント表は828pxの枠に1107pxで、確定報酬が左端6pxしか
  見えず、型・計測は不可視（どのデスクトップでも常に）。PR #86 のトレードオフは正しいが、
  その先で何が起きるかは検証されていなかった
- **モバイルの予感は半分正しく半分外れ。**配色の乏しさは同じ（レスポンシブなCSSは3か所だけ）。
  表のクリップはモバイルでは起きない（カード表示に変わるため）
- **前期間比（Buffer 式の↗7%）は、いまのコードでは出せない。**`untilMs` が存在しない。
  **同じ30日を割って擬似的な差分を作る案は却下**——「実績でない数字を実績のように見せる」
  欠陥（`CLAUDE.md` が名指しする生涯合計の件）と同じ形
- **アイコン案：**`channel.id` が製品自身が出荷・文書化している名前（`threads`・`x`・`note`・
  `youtube`）と一致するときだけ印を出し、それ以外は常に同じ「わからない」印にする。
  **本物のブランドロゴは描かない**（商標・追従コスト）／**絵文字は使わない**（描画が揃わない）。
  オーナー自身が「今回思いついた新しいアイデア」と明言しているので、**「あとで」**に置いた

### 私が最初に置いた前提の誤り

「`ChannelId` は一般文字列でどのSNSか分からないので、ブランドアイコンは常に不誠実」と
提案書の担当に書き送ったが、**半分しか正しくなかった。**出荷される設定ファイルに
`id: x`・`id: note`・`id: youtube` の雛形が実際にあり、`integrations.md` §3 がそれを
文書化している。オーナーの「対応してないのは今だけで、いずれ対応するのでは」という指摘が
正しかった。**「設定が実際に知っていることだけ、正直に出す」という線に直した。**

### 検証できなかったこと（提案書に明記済み）

`amp-test` 実機（ログイン壁）／手渡しカードの実機（承認操作をこのセッションの安全機構が
自己承認として正しく拒否した）／モバイルで長い数字が折り返した先の整列。

### 進め方の決定（同日、オーナー）

- 実装・レビューは Sonnet／Haiku に切り、親（Opus）は仕様・差分の読み・判断に専念する
- 画面のことは、親が自分でブラウザを操作して確かめる。伝言に頼らない


---

## 2026-09-26 A8.net のリダイレクト：ウェブで調べた。公式の禁止は見つからず、ただし「リンクのみ使用」の禁止と、未検証の報告が1件ある

### 調べたもの（一次情報を直接読んだ）

- [禁止事項](https://www.a8.net/compliance/prohibited-matter.php)：**リダイレクト・転送・短縮URL・中継ページの語は無い。**
  ただし「**広告コードからリンク部分のみを使用すること**」が、広告コードの無断変更として
  **明示的に禁止**されている（バナー画像・文言の変更と並べて）
- [広告の掲載方法](https://support.a8.net/a8/as/faq/manual/how_to_post.php)：リンクのみの掲載・
  リダイレクト・広告コード内の1pxトラッキング画像の扱いについて、**記載なし**

### 二次情報（未検証）

解説ブログ [webkaihatsu.com](https://webkaihatsu.com/a8-redirect/) が、A8サポートの返答として
「**アフィリエイトリンクのリダイレクトはご遠慮ください**」と書いている。**日付も問い合わせ内容も
出典も無い。**公式の記述ではなく、著者が「リンク部分のみを使用」に該当すると**推測**で結びつけている。
「ご遠慮ください」は禁止より弱い言い方でもある。

🔴 **私の最初の検索結果の要約は「A8はリダイレクト禁止」と断定していたが、それはこのブログの
主張をなぞっただけで、一次情報の裏付けは無かった。**一次を読んで訂正した。

### 分かったこと・まだ分からないこと

- **リダイレクト自体を名指しで禁じる公式の条文は、見つからなかった。**Amazon（第6条(v)・料率表2(e)）
  のように「報酬が出ない」と明記されているものも、見つからなかった
- ただし**サポートが「ご遠慮ください」と答えたという報告が1件ある**ので、**安全とも言えない**
- 🔴 **別の問題：「リンク部分のみを使用」の禁止は、リダイレクトの有無に関係なく、
  このプラットフォームの全投稿に当たりうる。**Threads の投稿には、広告コードの1pxトラッキング画像を
  付けられないため、リンクだけを載せる形になる。A8がSNSではこれを許しているのか、
  **一次では確認できていない**
- 未確認：A8には「広告掲載URL管理」があり、掲載ページを登録する仕組みがある。SNSの個別投稿URLや、
  自社ドメインの中継を経由したクリックが、これとどう突き合わされるか

### 結論

**設計はまだ変えない。**B2b（`direct` モード）は引き続き保留。**A8へ直接問い合わせる**——
今回は「ご遠慮ください」という具体的な返答を引用して訊ける。問い合わせ文面は下。

> 貴社の広告主の商品を、SNS（Threads）の投稿でご紹介する予定です。次の3点を教えてください。
>
> 1. 発行された成果測定リンクを、自社ドメインの中継URL（例：`https://自分のドメイン/go/xxxx`。
>    アクセスを1回記録してから、そのまま貴社のリンクへ即座に転送するのみ）経由で読者に届ける形式は、
>    認められますか。「アフィリエイトリンクのリダイレクトはご遠慮ください」というご案内を
>    見かけたのですが、これは禁止でしょうか。認められない場合、成果は承認されないのでしょうか
> 2. SNSの投稿には広告コードの画像タグ（1pxのトラッキング画像）を含められず、リンクのみを載せる形に
>    なります。禁止事項にある「広告コードからリンク部分のみを使用すること」に該当しますか。
>    SNSでの掲載方法として、貴社が想定・推奨している形があれば教えてください
> 3. 「広告掲載URL管理」への登録は、SNSの個別の投稿URLごとに必要ですか

---

## 2026-09-26 ASP側の数字を正にする考え方（オーナー）と、調べて分かったこと

### 問い

「Amazon や A8.net のようにプラットフォーム側で集計されていて、値を見たり API で取れるなら、
その方がいいのでは」。

### 答え：原則として正しい。お金の部分は既にそうなっている

成果・報酬は最初から ASP 側の数字で、このプラットフォームは数えず、CSV／Webhook で取り込むだけ。
こちらが数えているのはクリック（`/go/`）だけ。**足りないのは、ASP の数字を取り込む道**
（Cloudflare では `csv` アダプタが動かない＝未決事項18）。

### 調べたこと（未検証を含む）

- **A8.net：**クリック数はレポートに出る。**リンクの末尾に `&id1=値`（`id2`…も可）を付けると、
  その値別に成果を確認できる「パラメータ計測」がある**（サポート対象外の上級者向け、との記載）。
  リダイレクトなしで投稿ごとの成果を出せる仕組み。レポートの **API／CSV での取得は未確認**
  （「成果データ連携API」は Google 広告・Meta 広告等へ**送る**用途で、取得用ではなさそう）。
  `パラメータ計測の使い方` の全文は未読（検索結果の要約だけ）
- **Amazon：**PA-API は商品情報用。成果レポートを取る API は**見つからなかった**。
  トラッキングIDの上限等は未確認

### これで `direct` モードの形が見える

**直リンク＋投稿ごとのID＋ASP レポートの取り込み。**`/go/` を通さなくても投稿ごとの成果が出れば、
Amazon の禁止・A8 の「ご遠慮ください」の両方を避けられる。代償：レポートは遅れて届く／
API が無ければ画面から CSV を上げてもらう／「クリック0件＝要レビュー」は別の見方が要る。

### 🔴 疑わしい点（未検証・直していない）

`platform.config.example.yaml` の A8 雛形が `subIdParam: a8mat` で、`csv.ts`／`webhook.ts` は
`url.searchParams.set(subIdParam, subId)` で付ける。**`set` は同名を上書きする。**A8 のリンクは
`…?a8mat=XXXX` で、`a8mat` は **A8 自身の広告素材ID**。上書きすると A8 のリンクが壊れるはず。
A8 が案内する自分用の欄は `id1`。**実際にクリックして確かめるまで、疑いとして置く。**

### 次

①A8「パラメータ計測」全文と CSV の有無を読む ②Amazon のレポート出力を調べる
③結果を踏まえ `direct` モードを「ASP レポート取り込み前提」で設計し直す（まだ設計は変えない）。
